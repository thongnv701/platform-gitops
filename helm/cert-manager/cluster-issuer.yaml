apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: "thongnv701@gmail.com"  # Update this with your email
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - dns01:
        cloudflare:
          email: "thongnv701@gmail.com"  # Update this with your Cloudflare email
          apiKeySecretRef:
            name: cloudflare-api-key
            key: api-key
      selector:
        dnsZones:
          - "thongit.space"