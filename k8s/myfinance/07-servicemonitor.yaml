apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: myfinance-api
  namespace: myfinance
  labels:
    app.kubernetes.io/name: myfinance-api
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: myfinance-api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
